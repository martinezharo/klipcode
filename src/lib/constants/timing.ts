/** Shared timing constants for debounce intervals across the app. */
export const DEBOUNCE_MS = 800;

/** How long a copy control stays in its "copied" state before reverting. */
export const COPY_FEEDBACK_MS = 2000;
